# ─────────────────────────────────────────────────────────────────────────────
# Stable proxy service — KubeRay creates head/serve services with random suffixes
# (e.g. ray-serve-minimax-xdssr-head-svc). This service targets the Ray head pod
# directly via labels so other components can use a predictable name.
# ─────────────────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: minimax-llm-svc
  namespace: default
  labels:
    app: minimax-llm
spec:
  selector:
    app: minimax
    ray.io/node-type: head
  ports:
    - name: serve
      port: 8000
      targetPort: 8000
      protocol: TCP
    - name: dashboard
      port: 8265
      targetPort: 8265
      protocol: TCP
  type: ClusterIP
---
apiVersion: ray.io/v1
kind: RayService
metadata:
  name: ray-serve-minimax
spec:
  serveConfigV2: |
    applications:
    - name: minimax
      import_path: ray.serve.llm:build_openai_app
      route_prefix: "/"
      args:
        llm_configs:
        - model_loading_config:
            model_id: minimax-m2.5
            model_source: /model-cache
          engine_kwargs:
            dtype: bfloat16
            max_model_len: 65536          # Start conservative; model native context is larger
            gpu_memory_utilization: 0.92  # 4x96GB = 384GB VRAM, aggressive utilisation is fine
            tensor_parallel_size: 4       # All 4 Blackwells on the large node
            trust_remote_code: true       # Required for minimax_m2 architecture
            disable_log_stats: false
          deployment_config:
            autoscaling_config:
              min_replicas: 1
              max_replicas: 1   # One 4-GPU node; no horizontal scaling
              target_ongoing_requests: 8
              metrics_interval_s: 5
              look_back_period_s: 30
              upscale_delay_s: 15
              downscale_delay_s: 600
            max_ongoing_requests: 32
  rayClusterConfig:
    rayVersion: "2.54.0"
    enableInTreeAutoscaling: false  # Fixed single replica — no cluster autoscaling needed
    headGroupSpec:
      rayStartParams:
        num-cpus: "0"
        num-gpus: "0"
      template:
        metadata:
          labels:
            app: minimax
        spec:
          containers:
            - name: ray-head
              image: rayproject/ray-llm:2.54.0-py311-cu128
              env:
                - name: RAY_GRAFANA_HOST
                  value: "http://kube-prometheus-stack-grafana.kube-system.svc.cluster.local"
                - name: RAY_GRAFANA_IFRAME_HOST
                  value: "http://localhost:3000"
                - name: RAY_PROMETHEUS_HOST
                  value: "http://kube-prometheus-stack-prometheus.kube-system.svc.cluster.local:9090"
              ports:
                - containerPort: 8000
                  name: serve
                  protocol: TCP
                - containerPort: 8080
                  name: metrics
                  protocol: TCP
                - containerPort: 6379
                  name: gcs
                  protocol: TCP
                - containerPort: 8265
                  name: dashboard
                  protocol: TCP
                - containerPort: 10001
                  name: client
                  protocol: TCP
              resources:
                requests:
                  cpu: 2
                  memory: 16Gi
              volumeMounts:
                - name: dshm
                  mountPath: /dev/shm
          volumes:
            - name: dshm
              emptyDir:
                medium: Memory
                sizeLimit: 16Gi
    workerGroupSpecs:
      - replicas: 1
        minReplicas: 1
        maxReplicas: 1
        numOfHosts: 1
        groupName: minimax-gpu-group
        rayStartParams:
          num-gpus: "4"
        template:
          spec:
            initContainers:
              - name: model-download
                image: alpine:3.20
                command: ["/bin/sh", "-c"]
                args:
                  - |
                    set -e
                    echo "Installing s5cmd..."
                    wget -q -O /tmp/s5cmd.tar.gz \
                      "https://github.com/peak/s5cmd/releases/download/v2.3.0/s5cmd_2.3.0_Linux-64bit.tar.gz"
                    tar xzf /tmp/s5cmd.tar.gz -C /usr/local/bin && rm /tmp/s5cmd.tar.gz

                    echo "Downloading MiniMax-M2.5 from s3://${MODEL_BUCKET}/MiniMaxAI/MiniMax-M2.5/..."
                    s5cmd --endpoint-url "https://${S3_ENDPOINT_HOSTNAME}" \
                      sync "s3://${MODEL_BUCKET}/MiniMaxAI/MiniMax-M2.5/*" /model-cache/
                    echo "Model download complete."
                env:
                  - name: S3_ENDPOINT_HOSTNAME
                    valueFrom:
                      secretKeyRef:
                        name: obj-store-secret
                        key: endpoint_hostname
                  - name: AWS_ACCESS_KEY_ID
                    valueFrom:
                      secretKeyRef:
                        name: obj-store-secret
                        key: access_key
                  - name: AWS_SECRET_ACCESS_KEY
                    valueFrom:
                      secretKeyRef:
                        name: obj-store-secret
                        key: secret_key
                  - name: AWS_REGION
                    valueFrom:
                      secretKeyRef:
                        name: obj-store-secret
                        key: region
                  - name: MODEL_BUCKET
                    valueFrom:
                      secretKeyRef:
                        name: obj-store-secret
                        key: bucket
                volumeMounts:
                  - name: model-cache
                    mountPath: /model-cache
            containers:
              - name: ray-worker
                image: rayproject/ray-llm:2.54.0-py311-cu128
                env:
                  - name: HUGGING_FACE_HUB_TOKEN
                    valueFrom:
                      secretKeyRef:
                        name: hf-secret
                        key: hf_token
                resources:
                  limits:
                    memory: 256Gi
                    nvidia.com/gpu: "4"   # All 4 GPUs — only fits on the large node
                  requests:
                    cpu: 16
                    memory: 128Gi
                    nvidia.com/gpu: "4"
                volumeMounts:
                  - name: model-cache
                    mountPath: /model-cache
                  - name: dshm
                    mountPath: /dev/shm
            volumes:
              - name: model-cache
                emptyDir:
                  sizeLimit: 520Gi  # MiniMax-M2.5 is a large frontier MoE model
              - name: dshm
                emptyDir:
                  medium: Memory
                  sizeLimit: 64Gi
